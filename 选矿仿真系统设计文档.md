# 东鞍山选矿全流程DCS仿真系统设计文档
## 精矿品位软测量研究专用

**仿真边界**：球磨溢流输出 → 精矿品位化验结果  
**仿真目的**：生成含物理耦合的DCS时间序列数据，用于训练TFe软测量模型  
**版本**：v1.0

---

# 第一章 总体架构

## 1.1 三层仿真结构

```
第0层：外生扰动 d(t)          ← 不可测的"看不见的手"，驱动整个系统
         ↓
第1层：隐藏物理状态 x(t)       ← 真实物理过程（粒度、品位、温度场等）
         ↓
第2层：DCS可观测变量 y(t)      ← 训练特征X（含噪声、传感器故障、执行延迟）
         ↓（总时滞3~6小时）
第3层：精矿品位 TFe(t+τ)       ← 预测目标Y
```

**核心设计原则**：DCS变量不等于物理状态，而是物理状态经测量噪声、传感器漂移、执行机构滞后后的观测结果。

## 1.2 仿真流程拓扑

```
[球磨溢流](外部输入)
        ↓  ṁ_ball, ρ_ball, d₈₀_ball, f_{-200目}
  ┌─────────────────────────────────────────┐
  │              磁选段                      │
  │  弱磁(准静态) → 强磁前浓缩 → 强磁/扫强磁  │
  └──────────────────┬──────────────────────┘
                     ↓ 混磁精矿 ṁ_mag, g_mag
  ┌─────────────────────────────────────────┐
  │              塔磨段                      │
  │  给矿泵池 → 旋流器群 → 塔磨机(闭路)      │
  └──────────────────┬──────────────────────┘
                     ↓ 溢流 ṁ_ov, f_{-325目}
  ┌─────────────────────────────────────────┐
  │           浮选前浓缩                     │
  └──────────────────┬──────────────────────┘
                     ↓ 浓缩底流
  ┌─────────────────────────────────────────┐
  │     阴离子反浮选段（两系列并联）           │
  │  搅拌槽加药 → 粗选 → 精选                │
  │               ↕  扫选（逆流循环）         │
  └──────────────────┬──────────────────────┘
                     ↓
                精矿浓缩 → TFe(t+τ_lab)
```

## 1.3 全局时间参数

| 参数 | 值 | 说明 |
|------|----|------|
| 仿真步长 Δt | 60 s | 对应DCS典型采样周期 |
| 总仿真时长 | ≥ 30天 = 43200步 | 保证足够样本量 |
| 化验时滞 τ_lab | 120~240 min | 取样→化验→报结果 |
| 磁选→塔磨时滞 | 15~30 min | |
| 塔磨→浮选时滞 | 30~60 min | |
| 浮选内部时滞 | 80~120 min | 粗精扫各级累计 |

---

# 第二章 第0层：外生扰动过程

这是整个仿真的随机驱动源，所有DCS变量的波动最终源于此层。

## 2.1 扰动向量定义

$$\mathbf{d}(t) = [d_1(t),\ d_2(t),\ d_3(t),\ d_4(t)]^T$$

| 符号 | 物理含义 | 均值 | 慢漂移σ/步 | 是否进入DCS |
|------|---------|------|----------|-----------|
| d₁(t) | 球磨溢流TFe品位 | 31.5% | 0.05% | 否（隐藏） |
| d₂(t) | 碳酸铁含量(cFe) | 1.8% | 0.02% | 否（隐藏） |
| d₃(t) | 矿石可磨性系数 | 1.0 | 0.01 | 否（隐藏） |
| d₄(t) | 公共管网水压 | 0.4 MPa | 0.002 MPa | 间接（冲矿水压） |

## 2.2 扰动动力学方程

各分量服从带约束的相关随机游走（Ornstein-Uhlenbeck过程）：

$$d_i(t) = \bar{d}_i + \xi_i(t)$$

$$\xi_i(t) = \phi_i \cdot \xi_i(t-1) + \eta_i(t), \quad \eta_i \sim \mathcal{N}(0,\ \sigma_{\eta,i}^2)$$

均值回归参数 φᵢ ≈ 0.99（慢漂移，约100步回归一次），确保扰动在物理范围内。

**d₁和d₂的地质相关性**（碳酸铁高时原矿品位往往偏低）：

$$\text{Cov}(\eta_1, \eta_2) = -0.6 \cdot \sigma_{\eta,1} \cdot \sigma_{\eta,2}$$

## 2.3 球磨溢流作为仿真输入边界

球磨溢流的DCS输出（作为磁选的外部输入，不在本仿真建模范围内）：

| 输入量 | 符号 | 范围（基于文档） | 建模方式 |
|-------|------|--------------|--------|
| 球磨溢流量 | ṁ_ball | 251~280 t/h × 3系列 | AR(1) + 慢漂移 |
| 球磨溢流浓度 | ρ_ball | 34.5%~42.5% | AR(1) |
| 球磨溢流粒度d₈₀ | d₈₀_ball | −200目含量 78.4% | AR(1) |
| 超细粒含量 | f_{-25μm} | 54.19%（文档） | f(d₈₀_ball) |

超细粒含量与粒度的关系（反S形，粒度越细超细粒越多）：

$$f_{-25\mu m}(t) = \frac{f_{max}}{1 + e^{-k_{25}(d_{80,ball}^{ref} - d_{80,ball}(t))}} + \varepsilon_{f25}$$

---

# 第三章 磁选段仿真

## 3.1 弱磁选（准静态代数模型）

弱磁选动态极快，在流程级仿真中视为准静态：

$$g_{wmag}(t) = \frac{d_1(t) \cdot k_{wm,Fe}}{1 + k_{wm,Si} \cdot (1 - d_1(t))}$$

弱磁回收率：

$$\beta_{wm}(t) = \beta_{wm,0} \cdot \left(1 - k_{wm,f25} \cdot f_{-25\mu m}(t)\right)$$

**文档标定点**：给矿品位31.49% → 弱磁精矿51.29%，作业回收率45.23%，以此标定k_wm。

弱磁尾矿品位（进入强磁前浓缩）：

$$g_{wm,tail}(t) = \frac{d_1(t) \cdot \dot{m}_{ball}(t) - g_{wmag}(t) \cdot \beta_{wm}(t) \cdot \dot{m}_{ball}(t)}{\dot{m}_{ball}(t)(1 - \beta_{wm}(t))}$$

## 3.2 强磁前浓缩（动态缓冲）

浓缩机提供1~3分钟的液力缓冲，用一阶滞后模拟：

$$\dot{m}_{conc,out}(t) = \dot{m}_{conc,out}(t-1) + \frac{\Delta t}{\tau_{conc}}(\dot{m}_{wm,tail}(t) - \dot{m}_{conc,out}(t))$$

浓缩机底流浓度从弱磁尾矿的~24%提升至~45%：

$$\rho_{conc,out}(t) = \rho_{conc,target} + \varepsilon_{\rho}$$

## 3.3 强磁/扫强磁（核心物理模型）

### 3.3.1 磁场强度

$$B(t) = \mu_0 \cdot \frac{N_{coil} \cdot I_{exc}(t)}{l_{gap}}$$

其中 N_coil 为线圈匝数，l_gap 为磁路气隙长度（LHGC-3000设计参数）。

### 3.3.2 矿浆流速

$$v_{slurry}(t) = \frac{\dot{m}_{conc,out}(t) / \rho_{slurry}(t)}{A_{cross} \cdot (1 - f_{matrix})} \cdot h(f_{ring}(t))$$

其中 h(f_ring) 为转环频率对流速的修正函数（转速越快，矿浆实际停留时间越短）。

### 3.3.3 强磁回收率（磁力-曳力竞争核心方程）

$$\beta_{strong}(t) = \frac{1}{1 + \exp\!\left[-\lambda\left(\frac{\mu_0 \chi(d_p) \cdot B(t) \cdot \nabla B(t)}{\rho_{particle}} - \frac{18 \mu_{slurry}(t) \cdot v_{slurry}(t)}{d_p^2(t)}\right)\right]}$$

其中：
- χ(d_p)：颗粒磁化率，赤铁矿≈1.2×10⁻⁵，磁铁矿≈1.4×10⁻³
- d_p(t)：等效粒径，由 f_{-25μm}(t) 决定：d_p ≈ d_p,ref · (1 - f_{-25μm})^0.5
- ∇B：磁场梯度，正比于 I_exc 和介质几何参数

**关键非线性特性**：当 f_{-25μm} → 0.54（文档实测），d_p → 极小，曳力项爆炸式增大，β_strong 断崖式下降。

**文档标定点**：
- 强磁给矿品位23.91% → 强磁精矿40.73%，回收率67.99%
- 扫强磁：精矿31.67%，尾矿7.30%
- 以此标定 λ, χ 等参数

### 3.3.4 混磁精矿（磁选段输出）

弱磁精矿 + 强磁精矿 + 扫强磁精矿合并：

$$g_{mag}(t) = \frac{g_{wmag}(t)\dot{m}_{wm} + g_{strong}(t)\dot{m}_{strong} + g_{sweep}(t)\dot{m}_{sweep}}{\dot{m}_{wm} + \dot{m}_{strong} + \dot{m}_{sweep}}$$

**文档标定点**：混磁精矿品位43.84%，单体解离度67.51%。

## 3.4 磁选段DCS变量方程（12个）

### 3.4.1 励磁电压 `agg_mag_excit_voltage`

操作员设定量，慢变AR(1)过程：

$$V_{exc}(t) = \bar{V}_{exc} + \xi_V(t), \quad \xi_V(t) = \phi_V \xi_V(t-1) + \eta_V$$

**标定**：φ_V = 0.999（极稳定，文档描述），σ_ηV 极小，均值参考LHGC-3000额定励磁电压（60~120V DC）。

### 3.4.2 励磁电流 `agg_mag_excit_current`

由励磁电压和线圈热阻决定（存在热-电耦合）：

$$R_{coil}(t) = R_0 \cdot [1 + \alpha_{Cu}(T_{coil}(t) - T_0)], \quad \alpha_{Cu} = 0.00393\ ℃^{-1}$$

$$I_{exc}(t) = \frac{V_{exc}(t)}{R_{coil}(t)} + \varepsilon_I, \quad \varepsilon_I \sim \mathcal{N}(0,\ 0.01^2\ A^2)$$

**标定**：R₀ 由设备额定参数决定（V_nom / I_nom），T₀ = 20℃。

### 3.4.3 线圈温度 `agg_mag_coil_temp`

热力学一阶ODE，含焦耳热与自然对流散热：

$$\tau_{th}\frac{dT_{coil}}{dt} = I_{exc}^2(t) \cdot R_{coil}(t) - k_{cool}(T_{coil}(t) - T_{amb})$$

离散化：

$$T_{coil}(t) = T_{coil}(t-1) + \frac{\Delta t}{\tau_{th}}\left[I_{exc}^2(t)R_{coil}(t) - k_{cool}(T_{coil}(t-1) - T_{amb})\right] + \varepsilon_T$$

**标定**：τ_th ≈ 1800s（热时间常数30分钟），稳态温度 T_steady = T_amb + I²R/k_cool ≈ 60~80℃。

### 3.4.4 & 3.4.5 尾矿阀开度 `agg_mag_tailings_valve1/2`

液位PID控制器输出（非自由变量）：

$$e_L(t) = L_{setpoint} - L_{measured}(t)$$

$$u_v(t) = K_p e_L(t) + K_i \sum_{k=0}^{t} e_L(k)\Delta t + K_d \frac{e_L(t) - e_L(t-1)}{\Delta t}$$

级联逻辑（二号阀在一号阀全开后才开）：

$$u_{v1}(t) = \text{clip}(u_v(t),\ 0,\ 1)$$
$$u_{v2}(t) = \text{clip}(u_v(t) - 0.5,\ 0,\ 1) \cdot \mathbb{1}[u_v(t) > 0.5]$$

**文档特征**："剧烈波动"——通过传感器漂移项复现：

$$L_{measured}(t) = L(t) + b(t) + \varepsilon_L, \quad b(t) = b(t-1) + \eta_b$$

**标定**：K_p, K_i, K_d 通过使阀门震荡频率匹配真实数据特征来标定。σ_b > σ_L（漂移幅度大于测量噪声）。

### 3.4.6 排污阀 `agg_mag_blowdown_valve`

周期性脉冲操作：

$$u_{blow}(t) = \begin{cases} u_{on} & t \bmod T_{blow} < \Delta t_{blow} \\ 0 & \text{otherwise} \end{cases} + \varepsilon_{blow}$$

**标定**：T_blow ≈ 28800s（8小时一班），Δt_blow ≈ 300~600s（5~10分钟）。

### 3.4.7 脉动电机频率 `agg_mag_pulsation_freq`

操作员设定量，极稳定AR(1)：

$$f_{pul}(t) = \bar{f}_{pul} + \xi_{pf}(t), \quad \phi_{pf} = 0.999, \quad \sigma_{pf} \text{ 极小}$$

**标定**：均值参考LHGC-3000设计参数（约200~400次/分钟）。

### 3.4.8 转环频率 `agg_mag_ring_freq`

操作员设定量，独立慢变AR(1)：

$$f_{ring}(t) = \bar{f}_{ring} + \xi_{rf}(t)$$

**对TFe的物理路径**：f_ring↑ → v_slurry↑ → 曳力↑ → β_strong↓ → g_mag↓ → TFe↓。

### 3.4.9 选矿液位 `agg_mag_level`

质量守恒ODE（关键动态状态）：

$$A_{tank}\frac{dL(t)}{dt} = Q_{in}(t) - Q_{conc}(t) - Q_{tail}(t)$$

$$Q_{in}(t) = \dot{m}_{conc,out}(t) / \rho_{slurry}(t)$$

$$Q_{conc}(t) = k_{conc} \cdot \max(L(t) - L_{overflow},\ 0)$$

$$Q_{tail}(t) = C_v \cdot (u_{v1}(t) + u_{v2}(t)) \cdot \sqrt{\max(L(t),\ 0) \cdot 2g}$$

DCS读数（含漂移）：

$$L_{DCS}(t) = L(t) + b(t) + \varepsilon_L$$

**标定**：A_tank 由设备容积估算，k_conc 由稳态液位和稳态给矿量标定。

### 3.4.10 冲矿水压力 `agg_mag_flush_water_pressure`

$$P_{flush}(t) = d_4(t) - k_{pipe} \cdot Q_{flush}^2(t) + \varepsilon_{Pf}$$

**标定**：k_pipe 由管路特性估算，Q_flush 为操作员手动设定的准恒定量。

### 3.4.11 主电机A相电流 `agg_mag_motor_current_rc`

$$I_{motor}(t) = I_0 + k_{mf}\dot{m}_{in}(t) + k_{mr}f_{ring}(t) + k_{mm}\beta_{strong}(t)\dot{m}_{in}(t) + \varepsilon_{Im}$$

**对TFe的物理路径**：电流包含 β_strong 信息，是TFe的间接指示量。

### 3.4.12 主电机BC线电压 `agg_mag_motor_voltage_rc`

$$V_{BC}(t) = V_{nom} + \delta V_{grid}(t) + \varepsilon_V, \quad \delta V_{grid}(t) = \phi_g \delta V_{grid}(t-1) + \eta_g$$

**标定**：V_nom = 380V，σ_g ≈ 1~3V（厂区电网波动）。

---

# 第四章 塔磨段仿真

## 4.1 输入变量（来自磁选段输出）

| 符号 | 含义 | 数值范围 |
|------|------|--------|
| ṁ_mag(t) | 混磁精矿量 | 依总给矿量 |
| ρ_mag(t) | 混磁精矿浓度 | ~45% |
| g_mag(t) | 混磁精矿品位 | ~43.84% |
| f_{-325μm,in}(t) | 入磨超细粒含量 | ~62.74%（文档） |

## 4.2 核心隐藏物理状态

$$\mathbf{x}_{tm}(t) = [L_{pool},\ L_{ov},\ \rho_{slurry},\ d_{80,tm},\ P_{mech},\ T_{b1},\ T_{b2},\ T_{sA},\ T_{sB},\ T_{red},\ T_{red,out}]^T$$

## 4.3 给矿泵池液位动力学

$$A_{pool}\frac{dL_{pool}}{dt} = Q_{mag,in}(t) + Q_{tm,disch}(t) + Q_{pool,water}(t) - Q_{pump,out}(t)$$

离心泵流量特性（转速-流量-扬程方程）：

$$Q_{pump,out}(t) = k_{pump} \cdot f_{pump}(t) \cdot \sqrt{\max(L_{pool}(t),\ 0)} + \varepsilon_{Qp}$$

塔磨排矿返回泵池（闭路）：

$$Q_{tm,disch}(t) = (1 - \alpha_{ov}(t)) \cdot Q_{pump,out}(t - \tau_{mill})$$

其中 τ_mill 为塔磨研磨时间（约5~15分钟）。

## 4.4 旋流器分级

旋流器给矿压力：

$$P_{cyc}(t) = k_P \cdot \rho_{slurry}(t) \cdot \left(\frac{f_{pump}(t)}{f_{nominal}}\right)^2$$

分流比（溢流率，文档标定：分级质效率24.81%，返砂比2.12~5.00）：

$$\alpha_{ov}(t) = \alpha_0 + k_{\alpha d}(1 - e^{-d_{80,tm}(t)/d_{ref}}) - k_{\alpha P} \cdot P_{cyc}(t) + w_\alpha(t)$$

旋流器溢流量（进入浮选前浓缩）：

$$Q_{ov}(t) = \alpha_{ov}(t) \cdot Q_{pump,out}(t)$$

沉砂量（进入塔磨）：

$$Q_{sand}(t) = (1 - \alpha_{ov}(t)) \cdot Q_{pump,out}(t)$$

## 4.5 塔磨研磨动力学

塔磨出料粒度（基于Bond功指数的简化版粒群平衡模型）：

$$d_{80,disch}(t) = d_{80,sand}(t) \cdot \exp\!\left(-\frac{k_{mill} \cdot P_{mech}(t) \cdot \Delta t}{\dot{m}_{sand}(t) \cdot d_3(t)}\right)$$

超细粒含量（溢流）由分级和研磨共同决定：

$$f_{-325\mu m,ov}(t) = f_{-325\mu m,ov,0} + k_{f325}(P_{mech}(t)/P_{rated}) + \varepsilon_{f325}$$

**文档标定点**：
- 塔磨给矿−325目：62.74%
- 塔磨排矿−325目：70.09%
- 最终溢流−325目：93.29%（设计要求≥92.5%）

## 4.6 塔磨机械负载功率

$$P_{mech}(t) = P_0 + k_{ms}\dot{m}_{sand}(t) + k_{md}(1 - f_{-325\mu m,sand}(t)) + k_{m\rho}\rho_{slurry,mill}(t) + w_P(t)$$

**标定**：CSM-1120额定1120kW，正常运行65~85%负载，即P_mech ∈ [730, 950] kW。

## 4.7 塔磨段DCS变量方程（18个）

### 4.7.1 泵池液位 `agg_tm_cyclone_pool_level`

$$L_{pool,DCS}(t) = L_{pool}(t) + b_L(t) + \varepsilon_L$$

$$b_L(t) = b_L(t-1) + \eta_b, \quad \eta_b \sim \mathcal{N}(0,\ \sigma_b^2)$$

**标定**：σ_b 调整至使液位"剧烈波动"特征与真实数据匹配。

### 4.7.2 泵池水阀位给定 `agg_tm_cyclone_pool_valve_setpoint`

$$u_{pool,sp}(t) = \text{clip}\left(\bar{u}_{pool} + k_{PID}(L_{set} - L_{pool,DCS}(t)),\ 0,\ 1\right)$$

### 4.7.3 泵池加水流量 `MC1_FET503_AI`

$$Q_{pool,water}(t) = C_{v,pool} \cdot u_{pool,sp}(t) \cdot \sqrt{d_4(t)} + \varepsilon_{Qw}$$

**标定**：C_v,pool 由阀门全开时的设计流量标定。

### 4.7.4 旋流器给矿管道流量 `agg_tm_cyclone_feed_flow`

$$Q_{cyc,feed,DCS}(t) = Q_{pump,out}(t) + k_{Lf}\frac{dL_{pool}}{dt}\bigg|_t + \varepsilon_{Qf}$$

**标定**：k_Lf 复现"高频宽幅震荡"特性，σ_Qf 参考真实流量计精度（约±0.5%FS）。

### 4.7.5 三旋给矿泵频率 `agg_tm_cyclone_pump_freq`

$$f_{pump}(t) = f_{pump}(t-1) + \Delta f_{manual}(t) + k_{fb}(L_{set} - L_{pool,DCS}(t)) + \eta_f$$

**标定**：正常运行范围30~50Hz，σ_ηf ≈ 0.1Hz。

### 4.7.6 三旋给矿泵电流 `agg_tm_cyclone_pump_current`

泵的水力功率：

$$H_{pump}(t) = a_0 - a_1(f_{pump}(t)/f_{nom})^2 Q_{pump,out}^2(t)$$

$$P_{pump}(t) = \frac{\rho_{slurry}(t) \cdot g \cdot H_{pump}(t) \cdot Q_{pump,out}(t)}{\eta_{pump}}$$

$$I_{cyc,pump}(t) = \frac{P_{pump}(t)}{\sqrt{3} V_{line} \cos\phi_{pump}} + \varepsilon_{Ip}$$

**标定**：泵特性曲线参数a₀, a₁由泵型号额定参数估计，η_pump ≈ 0.75~0.85。

### 4.7.7 沉砂水阀位给定 `agg_tm_cyclone_sand_valve_setpoint`

操作员间歇调整（泊松阶跃过程）：

$$u_{sand,sp}(t) = \bar{u}_{sand} + \sum_k \Delta u_k \cdot \mathbb{1}[t \geq t_k]$$

**标定**：每班（28800s）调整2~4次，每次调整幅度Δu_k ~ Uniform(−0.1, 0.1)。

### 4.7.8 沉砂水阀位反馈 `agg_tm_cyclone_sand_valve_feedback`

执行机构一阶跟踪：

$$u_{sand,fb}(t) = u_{sand,fb}(t-1) + \frac{\Delta t}{\tau_{act}}(u_{sand,sp}(t) - u_{sand,fb}(t-1)) + \varepsilon_{fb}$$

**标定**：τ_act ≈ 10~30s（电动调节阀执行机构时间常数）。

### 4.7.9 旋流器沉砂加水流量 `agg_tm_cyclone_sand_water_flow`

$$Q_{sand,water}(t) = C_{v,sand} \cdot u_{sand,fb}(t) \cdot \sqrt{d_4(t)} + \varepsilon_{Qs}$$

**对TFe的物理路径**：Q_sand_water → ρ_slurry,mill → P_mech → f_{-325μm,ov} → TFe。

### 4.7.10 塔磨主电机电流 `agg_tm_motor_current`

$$I_{motor}(t) = \frac{P_{mech}(t)}{\sqrt{3} V_{line} \cos\phi_{motor}} + \varepsilon_{Im}$$

**这是塔磨段对TFe预测价值最高的变量**（直接反映研磨强度）。

### 4.7.11 & 4.7.12 滑动轴承温度 `MC1_TM204/TM206_HDZC_1/2_WD_AI`

$$\tau_{b1}\frac{dT_{b1}}{dt} = k_{b1} P_{mech}(t) \omega_{screw}^2 (1 + k_{wear}\cdot t_{aging}) - h_{b1}(T_{b1}(t) - T_{amb}) + w_{b1}$$

$$T_{b1,DCS}(t) = \begin{cases} T_{b1}(t) + \varepsilon_{b1} & \text{概率}(1 - p_{fault}) \\ -287.04 & \text{概率}\ p_{fault} \end{cases}$$

轴承2的结构相同，热参数略有差异（τ_b2 ≠ τ_b1）。

**标定**：p_fault ≈ 0.002（文档描述存在极端错误值），k_wear·t_aging 模拟设备老化缓慢趋势。

### 4.7.13 & 4.7.14 主电机定子温度 `MC1_TM204/206_ZDJ_DZ_A/B_WD_AI`

$$\tau_{sA}\frac{dT_{sA}}{dt} = k_{sA} I_{motor}^2(t) R_{stator} - h_{sA}(T_{sA}(t) - T_{coolant}) + w_{sA}$$

$$T_{sB}(t) = T_{sA}(t) + \Delta T_{AB} + \varepsilon_{AB}$$

定子B与A存在固定偏置（约1~3℃）。传感器异常处理同轴承温度（p_fault同量级）。

**标定**：τ_sA ≈ 600~1200s（定子热时间常数），T_coolant ≈ 25~35℃（随季节慢变）。

### 4.7.15 减速机油池温度 `agg_tm_reducer_oil_temp`

$$\tau_{red}\frac{dT_{red}}{dt} = k_{red} P_{mech}(t)\frac{1-\eta_{red}}{\eta_{red}} - h_{red}(T_{red}(t) - T_{amb}) + w_{red}$$

**标定**：η_red ≈ 0.96，τ_red ≈ 1800~3600s，稳态油温45~65℃。

### 4.7.16 减速机出油口温度 `agg_tm_reducer_outlet_temp`

$$T_{red,out}(t) \approx \alpha_{pipe} T_{red}(t) + (1-\alpha_{pipe})T_{amb} + \varepsilon_{out}$$

**标定**：α_pipe ≈ 0.90~0.95（管路散热衰减系数）。

### 4.7.17 旋流器溢流泵池液位 `agg_tm_cyclone_overflow_pool_level`

$$A_{ov}\frac{dL_{ov}}{dt} = Q_{ov}(t) - Q_{ov,pump}(t)$$

$$Q_{ov,pump}(t) = \bar{Q}_{ov} \cdot \mathbb{1}[L_{ov}(t) > L_{low}] + \varepsilon_{ov}$$

$$L_{ov,DCS}(t) = L_{ov}(t) + \varepsilon_{Lov}$$

**标定**："剧烈波动"来源于Q_ov(t)随旋流器分级效率的高频波动（文档：效率仅24.81%）。

### 4.7.18 旋流器溢流泵电流 `agg_tm_overflow_pump_current`

$$I_{ov,pump}(t) = I_{ov,0}\mathbb{1}[L_{ov}(t) > L_{low}] + k_{ov} Q_{ov,pump}(t) \rho_{ov}(t) + \varepsilon_{Iov}$$

**标定**：ρ_ov ≈ 1.10~1.15 g/cm³（文档：溢流浓度约14.93%）。

---

# 第五章 浮选段仿真

## 5.1 浮选前浓缩（缓冲环节）

浓缩机将塔磨溢流从~14.93%提升至38~40%：

$$\frac{d\rho_{conc}}{dt} = \frac{1}{\tau_{NT}}(\rho_{target} - \rho_{conc}(t)) + w_{NT}$$

底流量（由液位控制）：

$$\dot{m}_{float,in}(t) = \rho_{conc}(t) \cdot Q_{NT,disch}(t)$$

浓缩机电流（两台：P2_N1_I, P3_N2_I）：

$$I_{NT,k}(t) = I_{NT,0} + k_{NT}\dot{m}_{solid,k}(t) \cdot \rho_{underflow,k}^{0.5} + \varepsilon_{NT}$$

## 5.2 浮选系统拓扑（两系列结构）

两系列（X1/X2）结构对称，参数允许5~10%差异（设备老化程度不同）。

**流向拓扑**：

```
给矿 → [CX1] → [CX2] → [CX3]
                              ↓底流（精矿）
                           [JX] → 最终精矿
                              ↑↓ 精选尾矿逆流返回CX
              ←←(泡沫)←← [SX1] → [SX2] → [SX3] → 最终尾矿
              逐级底流逆流返回上一级
```

## 5.3 单槽通用物理状态

每个浮选槽 i 的隐藏状态：

$$\mathbf{x}_i(t) = [L_i,\ h_{froth,i},\ C_{Fe,i},\ C_{Si,i}]^T$$

### 5.3.1 槽内液位动力学（液位阀来源）

$$A_{tank}\frac{dL_i}{dt} = Q_{in,i}(t) - Q_{out,i}^{pulp}(t) - Q_{out,i}^{froth}(t)$$

底流液位阀PID（生成 AI3 液位阀实际值 + AI4 设定显示）：

$$u_{lv,sp,i}(t) = K_p(L_{sp} - L_i) + K_i\int(L_{sp}-L_i)dt$$

$$\dot{u}_{lv,i}(t) = \frac{u_{lv,sp,i}(t) - u_{lv,i}(t)}{\tau_{act,lv}}$$

$$Q_{out,i}^{pulp}(t) = C_{v,i} u_{lv,i}(t)\sqrt{\max(L_i(t) - L_{ref},\ 0)}$$

### 5.3.2 泡沫层动力学（泡沫层厚度来源）

$$\frac{dh_{froth,i}}{dt} = k_{gen,i} Q_{air,i}(t) C_{Si,i}(t) - k_{col,i} h_{froth,i}(t) - k_{scrape}\omega_{scraper}$$

铁矿物夹带损失率：

$$R_{Fe,ent,i}(t) = k_{ent} h_{froth,i}(t) C_{Fe,i}(t)\frac{Q_{air,i}(t)}{Q_{air,nom}}$$

泡沫层厚度DCS读数（含传感器底值故障）：

$$h_{froth,DCS,i}(t) = \begin{cases} h_{froth,i}(t) + \varepsilon_h & 1-p_{fault} \\ -21 & p_{fault} \end{cases}$$

**标定**：p_fault ≈ 0.005，对应变量：各系列 CX1/CX2/CX3/JX/SX1/SX2/SX3 的 AI1。

### 5.3.3 充气动力学（气量与蝶阀来源）

鼓风机出口压力（含两台并联）：

$$P_{blower}(t) = a_{bl,0} + a_{bl,1}\omega_{bl}^2 - a_{bl,2} Q_{total,air}^2(t) + \varepsilon_{Pb}$$

单槽单路气量（孔口流量方程，生成 AI5/AI9/AI13 气量实际值）：

$$Q_{air,i,j}(t) = C_d A_{orif} u_{bv,i,j}(t)\sqrt{\frac{2(P_{blower}(t) - P_{slot,i}(t))}{\rho_{air}}}$$

蝶阀开度（生成 AI7/AI11/AI16 蝶阀开度实际值）：

$$u_{bv,i,j}(t) = u_{bv,i,j}(t-1) + \eta_{bv}, \quad \eta_{bv} \sim \mathcal{N}(0,\ \sigma_{bv}^2)$$

气量设定值（生成 AI6/AI11/AI15 设定值显示，略先于实际值）：

$$Q_{air,sp,i,j}(t) = u_{bv,i,j}(t) \cdot Q_{air,max} + \varepsilon_{sp}$$

**标定**：P_blower 对应 PT_901_CK/PT_902_CK（约20~40kPa表压），σ_bv 由蝶阀实际调整频率估计。

### 5.3.4 矿物浓度动力学（品位演化核心）

脉石（硅）一阶浮选动力学：

$$\frac{dC_{Si,i}}{dt} = \frac{Q_{in,i}C_{Si,in,i} - Q_{out,i}C_{Si,i}}{V_i} - k_{Si,i}(t) C_{Si,i}$$

铁矿物（受控上浮，反浮选中铁矿物被抑制）：

$$\frac{dC_{Fe,i}}{dt} = \frac{Q_{in,i}C_{Fe,in,i} - Q_{out,i}C_{Fe,i}}{V_i} - R_{Fe,ent,i}(t)$$

**浮选速率常数（核心非线性方程）**：

$$k_{Si,i}(t) = k_{Si,0} \cdot \frac{Q_{TD,i}(t)^\alpha}{1 + e^{\beta(Q_{TD,i}(t) - Q_{TD}^*)}} \cdot \frac{pH_i(t)^\gamma}{1 + k_{Ca}C_{Ca^{2+}}(t)} \cdot \frac{Q_{air,i}(t)}{Q_{air,nom}}$$

**非单调性**：当 Q_TD > Q_TD* 时，k_Si 反而下降（文档："多加药导致尾矿跑高"）。

**文档标定点**：
- 粗选2100 g/t → 精矿67.43%，尾矿12.86%
- 粗选1500 g/t → 精矿66.56%，尾矿20.90%，回收率79.70%
- 以此两点标定 k_Si0, α, β, Q_TD*

## 5.4 加药网络DCS变量

### 5.4.1 加药泵（螺杆泵/化工泵）通用模型

适用于所有 LGB/HGB 系列泵的频率和电流变量：

**流量-频率关系（螺杆泵近似线性）**：

$$Q_{drug,k}(t) = k_{screw,k} \cdot f_{drug,k}(t) + \varepsilon_{Qd}$$

**电流-频率-负载关系**：

$$I_{drug,k}(t) = I_{0,k} + k_{If,k} f_{drug,k}(t) + k_{IQ,k}\rho_{drug,k} Q_{drug,k}(t) + \varepsilon_{Id}$$

**泵频控制**（操作员根据吨矿单耗目标设定，含反馈微调）：

$$f_{drug,k}(t) = f_{nom,k} + \Delta f_{manual,k}(t) + k_{fb,k} e_{drug,k}(t)$$

| DCS变量组 | 对应药剂 | 均值/范围（文档） |
|----------|--------|--------------|
| LGB2302/2303_F/I | TD-II捕收剂粗选 | 300~600 ml/s |
| LGB2401/2403/2404_F_W/I | TD-II捕收剂精选 | 200~300 ml/s |
| LGB2502_F | K6-1抑制剂粗选 | 100~200 ml/s |
| LGB2602_F/I | K6-1抑制剂一扫 | 50~100 ml/s |
| LGB2701/2702_F | NaOH | 70~100 ml/s（配置浓度20%） |
| LGB2801_F/I | 皂化泵TD-II粗选 | 配套主泵 |
| HGB2201~2204_F/I | CaO一次 | 300~450 ml/s（配置浓度2.5%） |

### 5.4.2 pH动力学

$$\frac{d[OH^-]}{dt} = \frac{Q_{NaOH}(t) C_{NaOH} - Q_{in}[OH^-]_{buffered}}{V_{tank}} - k_{buff}(t)[OH^-]C_{Si}(t)$$

$$pH(t) = 14 + \log_{10}([OH^-](t)) + \varepsilon_{pH}$$

碳酸铁对缓冲能力的扰动：

$$k_{buff}(t) = k_{buff,0} + k_{buff,c} d_2(t)$$

**标定**：文档数据 pH 稳定在9.2~10.1，σ_pH ≈ 0.05；K_buff,c 使碳酸铁增加1%时pH约下降0.1。

覆盖：`FX_AT_1102`（I系列），`FX_AT_2104`（II系列）。

### 5.4.3 搅拌槽温度系统

蒸汽加热ODE（生成 TT_xxxx 温度变量）：

$$\tau_{tank}\frac{dT_{tank,k}}{dt} = k_{steam} u_{TV,k}(t)(T_{steam} - T_{tank,k}) - h_{loss}(T_{tank,k} - T_{amb})$$

蒸汽阀PID（生成 TV_xxx_AO 给定 + TV_xxx_AI 反馈）：

$$u_{TV,sp,k}(t) = K_{p,TV}(T_{sp} - T_{tank,k}) + K_{i,TV}\int(T_{sp}-T_{tank,k})dt$$

$$u_{TV,fb,k}(t) = u_{TV,fb,k}(t-1) + \frac{\Delta t}{\tau_{TV}}(u_{TV,sp,k} - u_{TV,fb,k})$$

**标定**：药剂搅拌槽目标温度约40~60℃，τ_tank ≈ 300~600s。

### 5.4.4 搅拌槽电流（JBC系列）

$$I_{JBC,k}(t) = I_{JBC,0} + k_{JBC}\mu_{drug,k}(t) + \varepsilon_{JBC}$$

$$\mu_{drug,k}(t) = \mu_0 e^{E_a/(R T_{tank,k}(t))}$$

**标定**：μ₀ 和 E_a 按药剂类型（CaO乳液、KDF-I溶液等）的粘温特性估算。

### 5.4.5 K6-1贮药箱液位 `FX_LT_611`

$$\frac{dL_{K6}}{dt} = Q_{fill,K6}(t) - Q_{pump,K6}(t)$$

$$L_{K6,DCS}(t) = L_{K6}(t) + \varepsilon_{LK6}$$

## 5.5 泵池与流体输送DCS变量

### 5.5.1 各级泵池液位（LT系列）

每个泵池的质量守恒（以I系列为例）：

$$A_{pool,1601}\frac{dL_{1601}}{dt} = Q_{JX,froth}(t) + Q_{SX1,bot}(t) - Q_{ZJB1101}(t)$$

$$L_{1601,DCS}(t) = L_{1601}(t) + \varepsilon_{L1601}$$

液位报警（布尔变量）：

$$BJ_{1601}(t) = \mathbb{1}[L_{1601,DCS}(t) > L_{alarm,1601}]$$

各泵池对应关系：

| 变量 | 泵池 | 主要输入 | 主要输出 |
|-----|------|--------|--------|
| LT_1601 | I系精选泡沫+一扫底流 | JX泡沫+SX1底流 | 返CX |
| LT_1602 | I系粗选泡沫+二扫底流 | CX泡沫+SX2底流 | 给SX1 |
| LT_1603 | I系三扫底流 | SX3底流 | 给SX2 |
| LT_2602 | II系粗选泡沫+二扫底流 | 同上对称 | 给SX1 |
| LT_2603 | II系三扫底流 | 同上对称 | 给SX2 |
| LT_1701 | 浮选溢流泵池 | 所有溢流水 | 循环水系统 |

### 5.5.2 渣浆泵（ZJB系列）频率与电流

通用方程（适用于所有渣浆泵）：

$$Q_{ZJB,k}(t) = k_{ZJB,k} f_{ZJB,k}(t)\sqrt{L_{pool,k}(t)} + \varepsilon_{QZ}$$

$$I_{ZJB,k}(t) = I_{ZJB,0} + k_{If,ZJB} f_{ZJB,k}^2(t)\rho_{slurry,k} + \varepsilon_{IZ}$$

泵频由对应泵池液位PID驱动（或操作员手动设定）。

含传感器负值故障（文档描述负值问题）：

$$Q_{ZJB,DCS}(t) = \begin{cases} Q_{ZJB,k}(t) + \varepsilon & 1-p_{fault} \\ -12.5 & p_{fault} \end{cases}$$

### 5.5.3 管道流量计（FT系列）

$$FT_k(t) = Q_{ZJB,k}(t) + \varepsilon_{FT}$$

高效搅拌槽入矿流量（FT_1701/1702/2701/2702）：

$$FT_{tank,j}(t) = Q_{ZJB,feed}(t)\alpha_{split,j} + \varepsilon_{FT}$$

矿浆浓度（DT_2602）：

$$DT_{2602}(t) = \rho_w + (\rho_{ore} - \rho_w)C_{solid,CX}(t) + \varepsilon_{DT}$$

### 5.5.4 鼓风机出口压力（PT_901/902_CK）

$$P_{blower,k}(t) = a_0 + a_1\omega_{bl,k}^2 - a_2 Q_{total,air}^2(t) + \varepsilon_{Pb}$$

**标定**：两台鼓风机并联，额定出口压力20~40kPa表压，σ_Pb ≈ 0.5kPa。

### 5.5.5 电力变量

变压器总瞬时有功功率（AH5_AI6/AH6_AI6）：

$$P_{AH,k}(t) = \sum_i P_{motor,i}(t) + \sum_j P_{pump,j}(t) + \varepsilon_{PAH}$$

浮选机电流（FXJ803_I等）：

$$I_{FXJ,i}(t) = I_{FXJ,0} + k_{FXJ}\rho_{slurry,i}(t) + \varepsilon_{FXJ}$$

### 5.5.6 开关阀状态量（ZT系列）

$$ZT_k(t) \in \{0,1\}, \quad P(\text{切换}) = \lambda_{switch}\Delta t$$

**标定**：λ_switch 对应每班1~2次切换，即 λ ≈ 1/(14400) s⁻¹。

---

# 第六章 目标变量（精矿品位TFe）

## 6.1 精矿品位测量点位置

根据流程图和文档分析，精矿品位化验的取样点为**精矿浓缩机底流**（压滤前），即：

```
浮选精选槽 → 精矿浓缩机(NT-30) → [取样点] → 压滤机 → 最终精矿
```

化验结果对应变量：`y_fx_xin1`（新1#线），`y_fx_xin2`（新2#线）。

## 6.2 精矿品位生成方程

精矿品位由精选槽出口矿浆中铁矿物浓度决定：

$$TFe_k(t+\tau_{lab}) = \frac{C_{Fe,JX,k,out}(t)}{C_{Fe,JX,k,out}(t) + C_{Si,JX,k,out}(t)} \times 100\% + \varepsilon_{lab}$$

两线系统偏置：

$$TFe_{xin2}(t) = TFe_{xin1}(t) + \delta_{12} + \varepsilon_{cross}$$

**标定参数**：
- δ₁₂ ≈ 0.1~0.3%（两线设备差异导致的系统性偏置）
- σ_lab ≈ 0.1~0.15%（化验误差，来自GB/T 6730系列国标）
- τ_lab ≈ 120~240 min（化验室时滞，不同样次取随机值）

## 6.3 开环仿真的品位方差控制

真实数据TFe方差仅1.8（控制系统压缩了波动），仿真中通过以下方式扩大方差：

- **关闭加药量PID**：用持续激励信号（PRBS伪随机序列）代替稳态控制
- **扩大d₁扰动幅度**：σ_η1 扩大至3~5倍
- **允许pH波动**：将pH控制精度放宽

目标：使仿真TFe方差达到4~6（真实数据的2~3倍），R²预测可行性显著提升。

---

# 第七章 所有参数标定方法

## 7.1 标定数据来源优先级

| 优先级 | 来源 | 适用范围 |
|-------|------|--------|
| 1 | 文档考查数据（直接测量值） | 均值、工作点标定 |
| 2 | 真实DCS数据统计特性 | 噪声幅度、波动频率 |
| 3 | 设备额定参数（铭牌） | 电机额定值、泵特性 |
| 4 | 工业经验值 | 时间常数、热参数 |

## 7.2 分模块标定清单

### 磁选段标定

| 参数 | 标定方法 | 数据来源 |
|------|--------|--------|
| k_wm（弱磁富集比） | 强制满足：给矿31.49%→精矿51.29%，回收率45.23% | 文档考查数据 |
| λ（强磁逻辑函数斜率） | 强制满足：给矿23.91%→精矿40.73%，回收率67.99% | 文档考查数据 |
| R₀（线圈冷阻） | V_nom/I_nom（设备铭牌） | LHGC-3000规格 |
| τ_th（热时间常数） | 1800s（工业经验，30分钟回热） | 工业经验 |
| K_p,K_i,K_d（液位PID） | 调整至阀门震荡频率匹配真实数据 | 真实DCS数据 |
| σ_b（传感器漂移） | 调整至液位波动幅度匹配真实数据 | 真实DCS数据 |

### 塔磨段标定

| 参数 | 标定方法 | 数据来源 |
|------|--------|--------|
| k_mill（研磨速率常数） | 强制满足：给矿d₈₀→排矿−325目70.09% | 文档考查数据 |
| α₀,k_αd,k_αP（旋流器分流比） | 强制满足：返砂比2.12~5.00，分级效率24.81% | 文档考查数据 |
| P₀,k_ms,k_md（功率方程） | 强制满足：额定1120kW，运行730~950kW | 文档+设备铭牌 |
| τ_b1,τ_b2（轴承热时间常数） | 600~1200s（工业经验） | 工业经验 |
| τ_red（减速机热时间常数） | 1800~3600s（工业经验） | 工业经验 |
| p_fault（传感器故障概率） | 约0.002，使故障频率匹配真实数据 | 真实DCS数据 |

### 浮选段标定

| 参数 | 标定方法 | 数据来源 |
|------|--------|--------|
| k_Si0,α,β,Q_TD*（浮选速率常数） | 强制满足两个工作点（1500/2100 g/t） | 文档考查数据 |
| k_buff,k_buff,c（pH缓冲） | 强制满足：pH稳定在9.2~10.1 | 文档数据 |
| k_screw（螺杆泵流量系数） | Q_nom/f_nom（额定点标定） | 设备铭牌 |
| τ_act,lv（液位阀执行机构） | 10~30s（工业标准） | 工业经验 |
| C_v,i（液位阀Cv系数） | 由稳态流量-开度点标定 | 工艺设计值 |
| p_fault（泡沫层传感器故障率） | ~0.005，使-21底值出现频率匹配真实数据 | 真实DCS数据 |
| δ₁₂（两线系统偏置） | 由真实数据两线品位均值差计算 | 真实DCS数据 |

## 7.3 全局噪声参数标定原则

对所有测量噪声 ε，采用如下标定步骤：

1. 从真实DCS数据中，找出该变量最稳定的时段（工况稳定期）
2. 计算该时段的标准差 σ_stable
3. 令仿真噪声 σ_ε = σ_stable（保证噪声量级真实可信）
4. 对于故障读数，统计真实数据中异常值比例作为 p_fault

## 7.4 时间序列对齐与采样

| 仿真输出 | 采样周期 | 说明 |
|---------|---------|------|
| 所有DCS变量 | 60s | 对应实际DCS采集频率 |
| 精矿品位TFe | 每班1~2次（约4~8小时） | 对应化验频率 |
| 时间对齐 | 取距离化验时刻最近的DCS均值窗口 | 窗口宽度建议30~60分钟 |

---

# 第八章 仿真输出汇总

## 8.1 DCS变量总表

| 段 | 变量数 | 主要变量类型 |
|----|-------|-----------|
| 磁选段 | 12 | 励磁电气量、液位、阀门、温度 |
| 塔磨段 | 18 | 液位、流量、电流、温度（轴承/定子/减速机） |
| 浮选单槽控制 | ~80 | 液位阀、气量、蝶阀、泡沫层（两系列） |
| 搅拌槽加药 | ~45 | 泵频/电流、温度、pH、阀位 |
| 泵池与输送 | ~35 | 泵池液位、渣浆泵频率/电流、管道流量 |
| 电力与状态 | ~8 | 变压器功率、浮选机电流、开关阀状态 |
| **合计** | **~200** | |
| 目标变量 | 2 | y_fx_xin1, y_fx_xin2 |

## 8.2 仿真质量验收标准

仿真数据应满足以下条件方可用于模型训练：

| 验收项 | 标准 |
|-------|------|
| TFe品位均值 | 接近真实数据均值（约66~68%） |
| TFe品位方差（开环） | ≥ 4.0（信噪比足够） |
| 各DCS变量量纲 | 与真实数据一致 |
| 各变量相关结构 | 物理意义可解释的变量间相关性 |
| 时间自相关 | 关键变量ACF衰减速度与真实数据相似 |
| 传感器异常频率 | 与真实数据统计特性匹配 |
| 品位预测R²（开环数据） | 目标≥0.70，以验证仿真有效性 |

---

*文档结束*
*本文档基于东鞍山烧结厂工艺考查报告及三份DCS变量分析报告综合设计*
*所有数学公式均有明确的物理机理来源，参数标定均可追溯至文档考查数据或工业标准*

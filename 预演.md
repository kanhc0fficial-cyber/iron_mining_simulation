# 仿真系统预演（极简伪代码）

---

## 顶层流程

```
cfg = load_configs()
rng = RNGFactory(seed=42)
sim = Simulator(cfg, rng)

sim.warm_up(300 steps)        # 让各动态状态到达稳态

for t in 0..43199:
    bus = {}
    disturbance.step(bus)     # 写 _x_d1~4
    ball_mill.step(bus)       # 写 _x_m_ball, _x_d80, _x_f25
    mag_sep.step(bus)         # 写磁选DCS + _x_g_mag
    tower_mill.step(bus)      # 写塔磨DCS + _x_f325_ov
    flotation.step(bus)       # 写浮选DCS + y_fx_xin1/2
    writer.write(bus)         # 过滤 _x_ 前缀后落盘
```

---

## disturbance.step(bus)

```
# OU过程，d1/d2相关
[eta1, eta2] = cholesky(cov) @ randn(2)
xi_d1 = phi * xi_d1 + eta1;  d1 = clip(d1_mean + xi_d1, 0.25, 0.38)
xi_d2 = phi * xi_d2 + eta2;  d2 = clip(d2_mean + xi_d2, 0.01, 0.04)
xi_d3 = phi * xi_d3 + randn;  d3 = clip(d3_mean + xi_d3, 0.8, 1.2)
xi_d4 = phi * xi_d4 + randn;  d4 = clip(d4_mean + xi_d4, 0.3, 0.5)
bus[_x_d1..4] = d1..4
```

---

## ball_mill.step(bus)

```
d3 = bus[_x_d3]
m_ball  = AR1(m_ball,  phi=0.98) + slow_drift
rho_ball= AR1(rho_ball,phi=0.97) + noise
d80     = AR1(d80,     phi=0.97) + noise
f25 = f_max / (1 + exp(-k*(d80_ref - d80)))   # 反S形
bus[_x_m_ball, _x_rho_ball, _x_d80, _x_f25] = ...
```

---

## mag_sep.step(bus)

```
d1,f25,m_ball = bus[...]

# 励磁
V_exc = AR1(V_exc, phi=0.999)
R_coil = R0*(1 + alpha*(T_coil - T0))
I_exc  = V_exc / R_coil + noise
T_coil = euler_ode(T_coil, heat=I²R, cool=k*(T-Tamb), tau=1800)

# 弱磁（代数）
g_wmag   = d1*k_Fe/(1+k_Si*(1-d1))
beta_wm  = beta0*(1 - k_f25*f25)
m_wm_tail= m_ball*(1 - beta_wm)

# 前浓缩（一阶滞后）
m_conc_out = m_conc_out + dt/tau*(m_wm_tail - m_conc_out)

# 强磁（磁力-曳力 Sigmoid）
B = mu0*N*I_exc/l_gap
v_slurry = m_conc_out / (rho*A*(1-f_matrix)) * h(f_ring)
beta_strong = sigmoid(lambda*(B*dB/rho_p - 18*mu*v/dp²))
g_strong, g_sweep = ... (类似弱磁代数)

# 混磁精矿
g_mag = weighted_avg(g_wmag, g_strong, g_sweep)
m_mag = m_wm + m_strong + m_sweep

# 液位 ODE + PID → u_v1, u_v2（级联逻辑）
L = euler_ode(L, Q_in - Q_conc - Q_tail)
u_v = PID(L_setpoint, L + drift + noise)
u_v1 = clip(u_v, 0, 1);  u_v2 = clip(u_v-0.5,0,1) if u_v>0.5

# 排污阀：周期脉冲
u_blow = u_on if t%T_blow < dt_blow else 0

# 电机电流
I_motor = I0 + k_mf*m_in + k_mr*f_ring + k_mm*beta_strong*m_in + noise

bus[agg_mag_excit_voltage/current/coil_temp/level/valve1/2/...] = 各DCS量 + 噪声/漂移
bus[_x_g_mag, _x_m_mag] = g_mag, m_mag
```

---

## tower_mill.step(bus)

```
m_mag, g_mag = bus[_x_m_mag, _x_g_mag]

# 泵池液位 ODE
Q_return = discharge_buf.peek(tau_mill)   # 时滞返回量
L_pool = euler_ode(L_pool, Q_mag_in + Q_return + Q_water - Q_pump)
Q_pump = k_pump * f_pump * sqrt(L_pool)

# 旋流器分级
P_cyc = k_P * rho * (f_pump/f_nom)²
alpha_ov = alpha0 + k_d*(1-exp(-d80/d_ref)) - k_P*P_cyc + noise
Q_ov   = alpha_ov * Q_pump
Q_sand = (1-alpha_ov) * Q_pump

# 研磨
d80_disch = d80_sand * exp(-k_mill * P_mech * dt / (m_sand * d3))
f325_ov   = f325_0 + k_f*(P_mech/P_rated) + noise
discharge_buf.push(Q_pump)

# 功率
P_mech = P0 + k_ms*m_sand + k_md*(1-f325_sand) + k_rho*rho_mill + noise

# 热力学（4个 FirstOrderThermal 实例）
T_b1  = thermal_b1.step(heat=P_mech*w²*(1+k_wear*t), T_amb)
T_b2  = thermal_b2.step(...)
T_sA  = thermal_sA.step(heat=I²*R_stator, T_coolant)
T_red = thermal_red.step(heat=P_mech*(1-eta)/eta, T_amb)
T_red_out = alpha_pipe*T_red + (1-alpha_pipe)*T_amb

# 故障注入
T_b1_DCS = inject_fault(T_b1, p=0.002, fault=-287.04)
T_sA_DCS = inject_fault(T_sA, p=0.002, fault=-287.04)

# 溢流泵池
L_ov = euler_ode(L_ov, Q_ov - Q_ov_pump)

bus[agg_tm_*] = 各DCS量 + 噪声
bus[_x_f325_ov, _x_Q_ov, _x_rho_ov] = f325_ov, Q_ov, rho_ov
```

---

## flotation.step(bus)

```
# 浮选前浓缩
rho_conc = euler_ode(rho_conc, (rho_target-rho_conc)/tau_NT + noise)
m_float_in = rho_conc * Q_NT_disch

# 两台鼓风机
P_blow = a0 + a1*w² - a2*Q_air_total² + noise

# 按拓扑顺序更新每个浮选槽（两系列分别迭代）
for series in [X1, X2]:
    for cell in [CX1, CX2, CX3, JX, SX1, SX2, SX3]:
        Q_in, C_Fe_in, C_Si_in = from_topology(bus, cell)

        # 充气 → 气量
        Q_air = C_d*A_orif*u_bv*sqrt(2*(P_blow - P_slot)/rho_air)

        # 液位 ODE + PID
        L = euler_ode(L, Q_in - Q_pulp - Q_froth)
        u_lv = PID(L_sp, L)

        # 泡沫层 ODE
        h_froth = euler_ode(h_froth, k_gen*Q_air*C_Si - k_col*h - k_scrape*w)
        R_Fe_ent = k_ent * h_froth * C_Fe * Q_air/Q_nom

        # 浓度 ODE
        k_Si_val = kSi(Q_TD, pH, Q_air, C_Ca)   # 非单调
        C_Si = euler_ode(C_Si, (Q_in*C_Si_in - Q_out*C_Si)/V - k_Si_val*C_Si)
        C_Fe = euler_ode(C_Fe, (Q_in*C_Fe_in - Q_out*C_Fe)/V - R_Fe_ent)

        # 泡沫层故障注入
        h_DCS = inject_fault(h_froth, p=0.005, fault=-21)
        bus[cell.AI1] = h_DCS; bus[cell.AI3] = u_lv; ...

# 加药网络
for pump in all_drug_pumps:
    Q_drug = k_screw * f_pump + noise
    I_drug = I0 + k_If*f + k_IQ*rho*Q + noise
    bus[pump.F, pump.I] = Q_drug+noise, I_drug

# pH 动力学
OH = euler_ode(OH, (Q_NaOH*C_NaOH - Q_in*OH_buff)/V - k_buff*OH*C_Si)
pH = 14 + log10(OH) + noise
bus[FX_AT_1102, FX_AT_2104] = pH_I, pH_II

# 搅拌槽温度
for tank in agitator_tanks:
    T = thermal.step(heat=k_steam*u_TV*(T_steam-T), T_amb)
    u_TV_sp = PID(T_sp, T);  u_TV_fb = lag_follow(u_TV_sp, tau_TV)
    bus[tank.TT, tank.TV_AO, tank.TV_AI] = T, u_TV_sp, u_TV_fb

# 化验（精选槽输出 → TFe，有随机时滞）
TFe_true = C_Fe_JX / (C_Fe_JX + C_Si_JX) * 100
TFe_xin1, TFe_xin2 = lab_assayer.step(TFe_true, t)  # 仅化验报出时刻返回非NaN

bus[y_fx_xin1, y_fx_xin2] = TFe_xin1, TFe_xin2
```

---

## 辅助工具伪代码

```
# PID（离散，带反积分饱和）
def PID.step(sp, meas):
    e = sp - meas
    integral += e*dt  (若输出未饱和)
    derivative = (e - e_prev)/dt
    u = clip(Kp*e + Ki*integral + Kd*derivative, u_min, u_max)
    e_prev = e
    return u

# FirstOrderThermal（前向欧拉）
def step(Q_heat, T_amb, dt):
    dT = (Q_heat - k_cool*(T - T_amb)) / tau
    T += dT*dt + noise
    return T

# RingBuffer
def push(v): buf[head] = v; head = (head+1)%cap
def peek(d): return buf[(head-d-1+cap)%cap]

# 故障注入
def inject_fault(v, p, fault_v):
    return fault_v if rand() < p else v
```

---

*预演用于验证 design.md 逻辑完整性，不作为可运行代码。*
